<launch>

    <!-- what config we are going to run (should match folder name) -->
    <arg name="verbosity"   default="INFO" /> <!-- ALL, DEBUG, INFO, WARNING, ERROR, SILENT -->
    <arg name="config"      default="euroc_mav" /> <!-- euroc_mav, tum_vi, rpng_aruco, kaist -->
    <!-- <arg name="config_path" default="$(find vio_frontend)/config/$(arg config)/estimator_config.yaml" /> -->
    <arg name="log_path" default="$(find vio_backend)/log" />

    <!-- mono or stereo and what ros bag to play -->
    <arg name="max_cameras" default="1" />
    <arg name="use_stereo"  default="false" />
    <arg name="bag_start"   default="0" /> <!-- v1-2: 0, mh1: 40, mh2: 35, mh3: 17.5, mh4-5: 15 -->
    <arg name="dataset"     default="MH_02_easy" /> <!-- V1_01_easy, V1_02_medium, V1_03_difficult, V2_02_medium, dataset-room1_512_16 -->
    <arg name="bag"         default="/home/gao/dataset/$(arg config)/$(arg dataset).bag" />

    <!-- saving trajectory path and timing information -->
    <arg name="dosave"      default="false" />
    <arg name="dotime"      default="false" />
    <arg name="path_est"    default="/tmp/traj_estimate.txt" />
    <arg name="path_time"   default="/tmp/traj_timing.txt" />

    <!-- if we should viz the groundtruth -->
    <arg name="dolivetraj"  default="false" />
    <!-- <arg name="path_gt"     default="$(find ov_data)/$(arg config)/$(arg dataset).txt" /> -->
    <arg name="visualize"   default="false" />

    <!-- MASTER NODE! -->
    <node name="vio_backend" pkg="vio_backend" type="vio_backend_node" output="screen" clear_params="true" required="true">

        <!-- bag parameters -->
        <param name="path_bag"                 type="str"    value="$(arg bag)" />
        <param name="bag_start"                type="double" value="$(arg bag_start)" />
        <param name="bag_durr"                 type="int"    value="-1" />

        <param name="img_width"                 type="int"    value="752" />
        <param name="img_height"                type="int"    value="480" />
        <param name="max_feat_n"                type="int"    value="225" />
        <param name="max_clone_pose"            type="int"    value="11" />

        <!-- master configuration object -->
        <param name="verbosity"                 type="str"    value="$(arg verbosity)" />

        <!-- world/filter parameters -->
        <!-- <param name="use_stereo"                type="bool"   value="$(arg use_stereo)" />
        <param name="max_cameras"               type="int"    value="$(arg max_cameras)" /> -->

        <!-- log parameters -->
        <param name="log_path"    type="str"    value="$(arg log_path)" />

    </node>

    <!-- <node name="frontend_recoreder" pkg="rosbag" type="record" args="/vio_frontend/vio_frontend_feat_0 -o $(find vio_frontend)/rosbag/features.bag"/> -->

    <!-- record the trajectory if enabled -->
    <!-- <group if="$(arg dosave)">
        <node name="recorder_estimate" pkg="ov_eval" type="pose_to_file" output="screen" required="true">
            <param name="topic"      type="str" value="/ov_msckf/poseimu" />
            <param name="topic_type" type="str" value="PoseWithCovarianceStamped" />
            <param name="output"     type="str" value="$(arg path_est)" />
        </node>
    </group> -->

    <!-- path viz of aligned gt -->
    <!-- <group if="$(arg dolivetraj)">
        <node name="live_align_trajectory" pkg="ov_eval" type="live_align_trajectory" output="log" clear_params="true">
            <param name="alignment_type" type="str" value="posyaw" />
            <param name="path_gt"        type="str" value="$(arg path_gt)" />
        </node>
    </group> -->

    <!-- <group if="$(arg visualize)">
        <node pkg="rviz" type="rviz" name="rviz" args="-d $(find ov_msckf)/launch/display.rviz"/>
    </group> -->


</launch>